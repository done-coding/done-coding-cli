# AI Agent 指令

**Claude Code 用户：** 无需读取本文件，项目指令在 `CLAUDE.md`（自动加载，含分层子目录 CLAUDE.md，按所在目录就近生效）。

**其他 AI 工具（Codex、Copilot、Gemini 等）：**

- 项目指令：`CLAUDE.md`（进入某目录时叠加该目录及其祖先链的 CLAUDE.md）
- 目录导航：各层 README 的 `repo-map` 段
